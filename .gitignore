# build/design artifacts
export/
*.zip

# OS
.DS_Store

# editor / notes
.obsidian/

# local tooling & source docs (ไม่ deploy)
export_to_obsidian.py
sync_commits.py
*.docx
Criteria_Master.csv

# security — ห้ามขึ้น GitHub เด็ดขาด (repo เป็น public + เสิร์ฟผ่าน GitHub Pages)
# รายงานช่องโหว่ที่ยังไม่ได้แก้ = แผนที่ให้ผู้โจมตี
docs/SECURITY_ASSESSMENT.md
*SECURITY_ASSESSMENT*
*PENTEST*
*.pentest.md

# secrets — Supabase MCP token ฯลฯ (repo public ห้ามหลุด)
.env
.env.*
*.token

# go-live readiness report — ไม่ขึ้น public (แผนที่ช่องโหว่)
*READINESS*
*GO_LIVE*
