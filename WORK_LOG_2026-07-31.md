# สรุปงาน — 5S Audit System

**วันที่:** 31 กรกฎาคม 2026
**Repo:** `Seksunw/5s-audit-system` (branch `main`)
**Live:** https://seksunw.github.io/5s-audit-system/

---

## หัวข้อหลัก: แก้บั๊ก submit คะแนน + เพิ่มระบบตัดคะแนน N/A + จัดหน้าเลือกพื้นที่ใหม่

ต่อเนื่องจากการย้าย backend ไป Supabase (30 ก.ค.) รอบนี้แก้บั๊กที่ทำให้ submit ไม่ได้
และเพิ่มฟีเจอร์ให้การให้คะแนนยุติธรรมและใช้งานง่ายขึ้น

---

## 1. แก้บั๊ก submit คะแนนล้มเหลว (400 Bad Request)

**อาการ:** กด submit ผลตรวจ → ขึ้น "บันทึก Details ล้มเหลว chunk 1"

**ต้นเหตุ:** ใน trigger `recalc_audit_header()` ประโยค `status = case ... end` คืนค่าเป็นชนิด
`text` แต่คอลัมน์ `audit_headers.status` เป็น enum `audit_status` — Postgres ไม่ cast ให้อัตโนมัติ
เกิด error `42804` → rollback ทั้ง transaction → POST audit_details ได้ 400

**แก้:** cast ผล CASE เป็น `::audit_status`
- `supabase/schema.sql` — แก้ trigger
- `supabase/migrations/2026-07-31_fix_recalc_status_cast.sql` — ไฟล์ fix (รันบน Supabase แล้ว)
- `js/app.js` — ปรับ toast ให้โชว์ error จริงจาก DB เวลา submit ล้มเหลว (debug ง่ายขึ้น)

## 2. เพิ่มตัวเลือก "ไม่มีในพื้นที่ (N/A)" ระดับหมวด

**ปัญหาเดิม:** คะแนนมีแค่ 0/1/2 ถ้าพื้นที่ไม่มีอุปกรณ์ (เช่นไม่มีตู้ยา) ข้อนั้นยังถูกนับใน
max_score โดยได้ 0 → เปอร์เซ็นต์เสียเปรียบทั้งที่ไม่ใช่ความผิด

**แก้:** เพิ่มปุ่ม "ไม่มีในพื้นที่" ที่**หัวหมวด** กดแล้วทุกข้อในหมวดถูกตัดออกจากการคำนวณ
- `audit_details` เพิ่มคอลัมน์ `na boolean` (`supabase/migrations/2026-07-31_add_na_exclude_from_score.sql` — รันแล้ว)
- trigger คำนวณใหม่ใช้ `sum(...) filter (where not na)` ตัด N/A ออกจากทั้ง total และ max
- `js/app.js` — ปุ่ม N/A ระดับหมวด, ฟังก์ชัน `toggleCategoryNA`, ปรับ progress/unanswered/submit ให้ข้าม N/A
- เพิ่ม i18n (ไทย/อังกฤษ) + CSS สถานะ N/A

## 3. อนิเมชั่นหุบข้อย่อยเมื่อตัด N/A

- กดตัดออก → ข้อย่อยหุบด้วยอนิเมชั่น (max-height transition ~0.28s) เหลือแค่หัวหมวด
- กดหัวหมวดเปิดดูข้อย่อยได้ (คลี่กลับ) แต่ยังตัดคะแนนอยู่ กดปุ่มซ้ำเพื่อยกเลิก N/A
- **แก้บั๊กแฝง:** ฟังก์ชัน `toggleCategory` ชื่อซ้ำกัน 2 ตัว (หน้า audit + criteria) ตัวหลังทับตัวแรก
  ทำให้กดหัวหมวดในหน้า audit ไม่พับ → เปลี่ยนชื่อหน้า audit เป็น `toggleAuditCategory`

## 4. แยกโรงอาหาร / ช่าง-ยูทิลิตี้ เป็นพื้นที่ส่วนกลาง

**แนวคิด:** โรงอาหารและช่าง/ยูทิลิตี้เป็นพื้นที่ที่มีในทุกโรงงาน ให้เข้าถึงตรงจากหน้าเลือกโรงงาน

- หน้า plant: เพิ่ม 2 การ์ด 🍽️ โรงอาหาร + 🔧 ช่าง/ยูทิลิตี้ (รวมทุกโรงงาน)
- กดการ์ด → หน้า area โหมดใหม่: ดึงพื้นที่ชนิดนั้นข้ามทุกโรงงาน จัดกลุ่มตามโรงงาน (SUP/POC/NIF)
- กดเลือกโรงงาน (SUP/POC/NIF) → รายการพื้นที่**ตัด** cafeteria/maintenance ออกแล้ว
- `getAreas` รับพารามิเตอร์ `areaType` เพิ่ม, `selectArea` ส่ง plantId/plantName ที่ถูกต้องต่อเข้าหน้าตรวจ
- **ไม่กระทบ DB** — โครงสร้างข้อมูลเดิม แค่เปลี่ยนวิธีแสดง/เข้าถึงบน UI
- หน้า admin (มอบหมายงาน) ยังดึงพื้นที่ครบทุกชนิดเหมือนเดิม

---

## Migration ที่ต้องรันบน Supabase (รันครบแล้ว)

| ไฟล์ | ทำอะไร |
|---|---|
| `2026-07-31_fix_recalc_status_cast.sql` | แก้ trigger cast `::audit_status` (บั๊ก 400) |
| `2026-07-31_add_na_exclude_from_score.sql` | เพิ่มคอลัมน์ `na` + trigger ตัด N/A ออกจากการคำนวณ |

## เวอร์ชันไฟล์

- `app.js` / `style.css` → `v=13`
- service worker cache → `v2.6`

## Commit หลัก

- `fix: cast status to audit_status in recalc trigger` — แก้บั๊ก 400
- `feat: เพิ่มปุ่มไม่มีในพื้นที่ (N/A) ระดับหมวด ตัดออกจากการคำนวณคะแนน`
- `feat: อนิเมชั่นหุบข้อย่อยเมื่อตัด N/A + แก้บั๊ก toggle หมวดชื่อซ้ำ`
- `feat: แยกโรงอาหาร/ช่าง-ยูทิลิตี้ เป็นพื้นที่ส่วนกลางบนหน้าเลือกโรงงาน` (`6d558ca`)

---

## ค้างไว้ทำต่อ

- โชว์ป้าย **N/A** ในหน้า summary/history (ตอนนี้คะแนนถูกแล้ว แต่ย้อนดูยังไม่เห็นว่าข้อ/หมวดไหนถูกตัด)
- ทดสอบ flow จริงบนเว็บหลัง deploy (การ์ด facility, การตัดพื้นที่ออกจากโหมดโรงงาน, การให้คะแนน N/A)
