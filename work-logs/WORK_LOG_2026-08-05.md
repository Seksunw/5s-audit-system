# สรุปงาน — 5S Audit System

**วันที่:** 5 สิงหาคม 2026
**Repo:** `Seksunw/5s-audit-system` (branch `main`)
**Live:** https://seksunw.github.io/5s-audit-system/

---

## หัวข้อหลัก: ปุ่ม "รีเซ็ตข้อมูลระบบ" ไม่เคยทำงานได้เลย — พบ 2 bug ซ้อนกัน

> **สรุป:** ตั้งใจแค่ล้างข้อมูลผลตรวจ แต่กลายเป็นค้นพบว่าฟังก์ชัน `admin_reset_data()`
> **พังมาตั้งแต่สร้างระบบ** และไม่มีใครรู้เพราะไม่เคยมีใครกดปุ่มจริง
>
> แก้ 2 bug · รีเซ็ตสำเร็จ · ข้อมูลไม่สูญหายระหว่างทาง (transaction rollback ทำงานถูก)

---

## 1. จุดเริ่ม — อยากล้างผลตรวจ

ข้อมูลก่อนล้าง: **41 ผลตรวจ · 2,770 รายละเอียดข้อ · 14 การมอบหมาย · 20 รูป**
คงไว้: ผู้ใช้ 16 · audit_logs 356

ก่อนลบได้ export CSV 3 ไฟล์เก็บไว้ (ผลตรวจ / รายละเอียด+หมายเหตุ+URL รูป / การมอบหมาย)

> ⚠️ **บทเรียนเรื่อง export:** SQL Editor จำกัด 100 แถวโดยปริยาย
> query รายละเอียด 2,770 แถวต้องเลือก **"No limit"** ก่อน export ไม่งั้นได้แค่ 100 แถว

---

## 2. 🐛 Bug ที่ 1 — เทียบ enum กับ `''`

**อาการ:** กดปุ่มแล้วขึ้น `invalid input value for enum user_role: ""`

**โค้ดเดิม** (`patches.sql:161` และ `schema.sql:336` — ทั้ง 2 ไฟล์):

```sql
if coalesce(public.auth_role(), '') <> 'admin' then
  raise exception 'permission denied: admin only';
end if;
```

**สาเหตุ:** `auth_role()` คืนชนิด `user_role` (enum) · `coalesce()` ต้องการให้ argument
ทุกตัวชนิดเดียวกัน → Postgres แปลง `''` เป็น `user_role` → **`''` ไม่ใช่ค่าที่ถูกต้องของ enum** จึงล้ม

**ผลกระทบ:** ฟังก์ชันล้มที่บรรทัดแรกเสมอ **ไม่ว่าจะเป็น admin หรือไม่**
→ ปุ่มรีเซ็ตใช้งานไม่ได้เลยตั้งแต่วันที่สร้าง (อธิบายได้ว่าทำไมไม่มีตาราง `*_backup` มาก่อน)

**แก้:**

```sql
if coalesce(public.auth_role()::text, '') <> 'admin' then
```

cast เป็น `text` ก่อน → กลายเป็นการเทียบ text กับ text ไม่ต้องแปลง `''` เป็น enum

> **หมายเหตุ:** จุดอื่นที่ใช้ `auth_role() = 'admin'` (RLS policies, `is_staff()`) **ปลอดภัย**
> เพราะ `'admin'` เป็นค่าที่มีอยู่ใน enum จริง · ปัญหาอยู่ที่ `''` เท่านั้น

---

## 3. 🐛 Bug ที่ 2 — Supabase บล็อก `delete from storage.objects`

**อาการ:** หลังแก้ bug 1 กดปุ่มอีกครั้ง ขึ้น
`Direct deletion from storage tables is not allowed. Use the Storage API instead.`

**สาเหตุ:** Supabase เพิ่มการบล็อก `DELETE` ตรงบน `storage.objects` แล้ว
บรรทัดนี้ในฟังก์ชันจึง raise:

```sql
delete from storage.objects where bucket_id = 'audit-photos';
```

**ข่าวดี — transaction rollback ทำงานถูกต้อง**

ลำดับในฟังก์ชันคือ: เช็คสิทธิ์ → นับ → สร้าง backup → **truncate** → **ลบรูป (ล้มที่นี่)**
plpgsql ทำงานใน transaction เดียว → raise แล้ว **rollback ทั้งหมด**

ยืนยันด้วย query หลัง error: `41 · 2770 · 14 · 20 · 0` — **ข้อมูลครบ ไม่มีตารางสำรองค้าง** ✓

**แก้ — ย้ายการลบรูปไปทำที่ client ผ่าน Storage API**

เอาบรรทัดลบรูปออกจาก SQL ทั้ง 3 ไฟล์ (`patches.sql` · `schema.sql` · `reset_test_data.sql`)
พร้อมคอมเมนต์เตือนไม่ให้ใส่กลับ · แล้วเพิ่มฟังก์ชันใหม่ใน `js/app.js`:

```js
async purgeAuditPhotos() {
  const B = CONFIG.STORAGE_BUCKET;
  let removed = 0, failed = 0;

  const walk = async (prefix) => {
    const { data:items, error } = await _sb.storage.from(B)
      .list(prefix, { limit: 1000, sortBy: { column:'name', order:'asc' } });
    if (error) { failed++; return; }

    const files = [], dirs = [];
    (items || []).forEach(it => {
      // โฟลเดอร์ไม่มี metadata / id
      if (it.id === null || it.metadata == null) dirs.push(it.name);
      else files.push(prefix ? `${prefix}/${it.name}` : it.name);
    });

    for (let i = 0; i < files.length; i += 100) {          // ลบชุดละ 100
      const chunk = files.slice(i, i + 100);
      const { error: rmErr } = await _sb.storage.from(B).remove(chunk);
      if (rmErr) failed += chunk.length; else removed += chunk.length;
    }
    for (const d of dirs) await walk(prefix ? `${prefix}/${d}` : d);
  };

  await walk('');
  return { removed, failed };
}
```

**จุดที่ต้องระวังในการเขียน:**

| เรื่อง | เหตุผล |
|---|---|
| ต้องเดินซ้ำทุกโฟลเดอร์ | `list()` คืนไฟล์เฉพาะชั้นเดียว แต่ path จริงคือ `audit/xxx.jpg` |
| แยกไฟล์กับโฟลเดอร์ด้วย `id === null \|\| metadata == null` | Supabase คืนโฟลเดอร์เป็น item ที่ไม่มี metadata |
| ลบเป็นชุดละ 100 | กัน payload ใหญ่เกิน |

`resetData()` ลบรูปก่อนแล้วค่อยเรียก RPC — ถ้าลบรูปล้มก็ยังรีเซ็ต DB ต่อได้ แค่รายงานจำนวนที่ลบไม่ได้
toast แสดงผลรูปด้วย: `รีเซ็ตแล้ว: ประวัติ 41 · มอบหมาย 14 · รูป 20 (สำรองไว้ที่ *_backup)`

---

## 4. ✅ ผลลัพธ์ — รีเซ็ตสำเร็จ

| ตรวจ | ผล |
|---|---|
| `audit_headers` · `audit_details` · `schedules` · รูป | **0 · 0 · 0 · 0** ✓ |
| `profiles` (คงไว้) | 16 ✓ |
| `audit_logs` (คงไว้) | 357 — เพิ่ม 1 จากรายการ `RESET` ที่ระบบบันทึก ✓ |
| ตารางสำรอง | **3 ตาราง** ✓ |
| `audit_headers_backup` · `audit_details_backup` · `schedules_backup` | **41 · 2770 · 14** ตรงกับก่อนลบ ✓ |
| RLS บนตารางสำรอง | `true` ทั้ง 3 · `authenticated` ไม่มีสิทธิ์ ✓ |

การล็อกตารางสำรองที่ทำไว้เมื่อ 4 ส.ค. (`4f4f371`) **ทำงานจริงตามที่ออกแบบ** — ยืนยันจากการรีเซ็ตครั้งแรกในชีวิตของระบบ

---

## 5. 🔑 บทเรียนสำคัญที่สุดของวันนี้

> **"โค้ดมีอยู่" ≠ "ทำงานได้"**

`admin_reset_data()` ผ่านการตรวจมาแล้ว **2 รอบ**:

1. รายงานประเมินความปลอดภัย 16 หัวข้อ (3 ส.ค.) — อ่านฟังก์ชันนี้ทั้งตัว ชมว่า
   "ออกแบบดี เช็คสิทธิ์ในตัวฟังก์ชัน ไม่พึ่ง client"
2. งานล็อกตาราง `*_backup` (4 ส.ค.) — แก้ไขในฟังก์ชันนี้โดยตรง ตรวจ `pg_proc` ยืนยันว่าอัปเดตแล้ว

**ทั้ง 2 รอบไม่เจอ bug** เพราะ:

- Bug 1 ต้อง **รัน** ถึงจะเห็น — static analysis อ่านโค้ดแล้วดูสมเหตุสมผล
- Bug 2 เป็นการเปลี่ยนแปลงฝั่ง Supabase ที่เกิดขึ้นภายหลัง — ไม่มีใน SQL ตัวเอง
- การตรวจว่า "ฟังก์ชันอัปเดตแล้ว" (เช็ค `prosrc`) ยืนยันแค่ **ข้อความในฟังก์ชัน** ไม่ได้ยืนยันว่า **เรียกได้**

**สิ่งที่ควรทำต่อจากนี้:**

- ปุ่ม/ฟังก์ชันที่ทำลายข้อมูล ควร **ทดสอบบน staging จริง** ไม่ใช่แค่อ่านโค้ด
- `Success` ของ SQL Editor และ `prosrc like '%...%'` **ไม่ใช่หลักฐานว่าใช้งานได้**
  ต้องเรียกฟังก์ชันจริงหนึ่งครั้ง
- ค่าที่ควรมี: smoke test เรียก RPC ทุกตัวหลัง deploy migration

---

## 6. ⚠️ ข้อจำกัดที่ยังเหลือ

**รูปภาพไม่ถูกสำรอง** — `*_backup` เก็บเฉพาะแถวใน DB
`audit_details_backup.photo_urls` ยังมี URL อยู่ แต่ไฟล์ถูกลบจาก Storage แล้ว → **กดแล้ว 404**

ถ้าต้องการสำรองรูปด้วย ต้องคัดลอกไฟล์ไป bucket อื่นก่อนลบ (Storage API `copy()`)
หรือ export ออกนอกระบบด้วยมือ — **ยังไม่ได้ทำ**

---

## 7. สถานะไฟล์

| ไฟล์ | สถานะ |
|---|---|
| `js/app.js` | ✏️ เพิ่ม `SBH.purgeAuditPhotos()` · `resetData()` ลบรูปก่อนเรียก RPC · toast รายงานจำนวนรูป |
| `supabase/patches.sql` | ✏️ แก้ enum cast + เอา `delete from storage.objects` ออก |
| `supabase/schema.sql` | ✏️ แก้เหมือนกัน (DB ใหม่ใช้ไฟล์นี้) |
| `supabase/reset_test_data.sql` | ✏️ comment บรรทัดลบรูป + เขียนวิธีลบ 3 ทางแทน |
| `sw.js` | ✏️ `v5.0` → `v5.1` → **`v5.2`** |
| 14 HTML | ✏️ แก้แค่ version → **`v=38`** |
| `css/style.css` | ไม่แตะ |

**รันบน Supabase แล้ว:** `create or replace function public.admin_reset_data()` เวอร์ชันแก้แล้ว ✓

---

# ส่วนที่ 2 — 🔐 จัดระเบียบสิทธิ์ให้เหลือ 3 roles + ล็อกผลตรวจ

## 8. การตัดสินใจเชิงนโยบาย

ระบบเดิมมี 4 roles ใน enum แต่ **`manager` กับ `area_manager` ไม่มีความหมายจริง**
(`area_manager` ไม่ถูกอ้างใน policy ไหนเลย · `manager` ถูก UI บล็อกแต่ RLS อนุญาต)

**ตัดสินใจ:** ใช้ 3 roles

| role | ความหมาย |
|---|---|
| 👑 `admin` | จัดการทุกอย่าง · แก้ผลตรวจได้ตลอด |
| 📋 `auditor` | ตรวจ 5ส · **ดูผลตรวจทั้งบริษัท** (Dashboard/ranking มีความหมายครบ) · แก้ผลตัวเองไม่ได้หลัง submit |
| 👁️ `viewer` | ผู้บริหาร — ดูได้ทุกอย่างรวม remark/รูป แต่ **ตรวจไม่ได้ แก้ไม่ได้** |

## 9. ⚠️ ข้อจำกัด: Postgres ลบค่าออกจาก enum ไม่ได้

มีแต่ `add value` ไม่มี `drop value` · การลบต้องสร้าง type ใหม่ + แปลงคอลัมน์
+ drop/create policy ทุกตัวที่อ้าง `user_role` = งานใหญ่เสี่ยงสูง

**วิธีที่เลือก:** `add value 'viewer'` แล้ว **เอา `manager`/`area_manager` ออกจาก dropdown**
→ เลือกไม่ได้อีก · คนที่เป็น role เก่าอยู่ยังใช้งานได้ต่อ (ได้สิทธิ์เท่า `auditor`) ไม่พัง

> ⚠️ `alter type ... add value` ต้อง **รันแยกก่อน** — ค่า enum ที่เพิ่งเพิ่มใช้ใน
> transaction เดียวกันไม่ได้ (จึงทำเป็นคอมเมนต์ในส่วน G0 ให้ uncomment รันเดี่ยว)

## 10. SQL — เพิ่มส่วน G ใน `patches.sql`

| ส่วน | ทำอะไร |
|---|---|
| G0 | `add value 'viewer'` (คอมเมนต์ไว้ — รันแยก) |
| **G1** | `headers_insert` → `auditor_id = auth.uid() and role <> 'viewer'`<br>**ปิด BOPLA ด้วย** — เดิม `is_staff()` ให้ admin/manager ปลอม `auditor_id` เป็นคนอื่นได้ |
| **G2** | `locked_at` + `headers_update` (auditor แก้ได้เฉพาะยังไม่ล็อก) + `headers_delete` (admin หรือยังไม่ล็อก) + trigger `trg_chk_locked` กันแก้ `audit_details` ของ audit ที่ล็อกแล้ว |
| **G3** | trigger `trg_log_details` — log การแก้ `score`/`na`/`remark` (เดิมไม่มีร่องรอยเลย) |
| **G4** | ลบ policy `schedules_auditor_update` ที่กว้างเกิน → RPC `mark_schedule_done(uuid)` แก้ได้แค่ `status` |
| G5 | `is_staff()` เหลือแค่ `admin` (ไม่มี manager แล้ว) |

**จุดที่ต้องระวังตอนเขียน:**

- `schedule_id` เป็น **uuid** ไม่ใช่ bigint — signature RPC ต้องตรง
- ส่วน A3 เดิมสร้าง `schedules_auditor_update` แล้ว G4 ลบทิ้ง → **comment A3 ออก**
  พร้อมอธิบายเหตุผล ไม่ให้คนอ่านสับสน
- ทุกที่ที่เทียบ role ใช้ `auth_role()::text` — บทเรียนจาก bug enum เมื่อเช้า

## 11. Client

| ไฟล์ | แก้อะไร |
|---|---|
| `users.html` | เอา `Manager`/`Area Manager` ออกจาก dropdown ทั้ง 2 จุด · ปรับคำอธิบาย role ให้ตรงสิทธิ์จริง |
| `js/app.js` | เพิ่ม `viewer` ใน `MAP.role` / `REV.role` · helper `currentRole()` `isAdminRole()` `isViewer()` |
| `js/app.js` | guard `isViewer()` ใน `initPlant` · `initArea` · `initAudit` · `initMyTasks` → เด้งกลับหน้าหลัก |
| `js/app.js` | `initHome()` ซ่อนปุ่ม "เริ่มตรวจ 5ส" + bottom-nav "ตรวจ" จาก viewer |
| `home.html` | เพิ่ม `id="startAuditBtn"` ให้ปุ่มเริ่มตรวจ (เดิมไม่มี id) |
| `js/app.js` | **`finalizeAudit()` ตั้ง `locked_at`** ← ถ้าลืมส่วนนี้ การล็อกทั้งหมดไร้ความหมาย |
| `js/app.js` | `completeSchedule()` เปลี่ยนจาก `.update()` → `rpc('mark_schedule_done')` |
| i18n | +`msg.viewer_no_audit` +`err.no_schedule_id` (th + en) |

**ลำดับใน `submitAudit()` สำคัญ** — details → **finalize (ล็อก)** → completeSchedule
ล็อกต้องเกิด *หลัง* อ่านคะแนนที่ trigger คำนวณไว้ · การล็อกล้มไม่ทำให้ submit ล้ม
(ผลตรวจบันทึกแล้ว) แค่ log warning

**ตรวจแล้วว่า `rollbackAudit()` ไม่ติดล็อก** — rollback เกิดก่อน finalize ทุกกรณี
`locked_at` ยัง null → policy `headers_delete` ผ่าน · cascade ลบ details ก็ผ่าน trigger

## 12. ทดสอบ 10 เคส ผ่านหมด

| เคส | ผล |
|---|---|
| viewer ถูกซ่อนปุ่มตรวจ | ✓ |
| viewer สร้างผลตรวจไม่ได้ | ✓ |
| auditor สร้างผลตรวจของตัวเองได้ | ✓ |
| **auditor ปลอม `auditor_id` เป็นคนอื่นไม่ได้** | ✓ |
| **admin ปลอม `auditor_id` ก็ไม่ได้ (ปิด BOPLA)** | ✓ |
| auditor แก้ผลตัวเองหลังล็อกไม่ได้ | ✓ |
| auditor แก้ผลตัวเองก่อนล็อกได้ (ตอน submit) | ✓ |
| admin แก้ผลที่ล็อกแล้วได้ | ✓ |
| auditor แก้ `audit_details` หลังล็อกไม่ได้ | ✓ |
| `manager` (role เก่า) ได้สิทธิ์เท่า auditor | ✓ |

+ syntax · HTML tag สมดุลทุกไฟล์ · i18n 83 key ครบ th/en

## 13. เก็บขยะที่ค้าง

พบ `js/i18n/th.js` ไฟล์เปล่า 0 byte ค้างจากงาน i18n ที่ยกเลิกไป 3 ส.ค.
ไม่มีหน้าไหนอ้าง → **ลบทิ้ง**

## 14. ต้องรันบน Supabase (2 ขั้น)

```sql
-- ขั้นที่ 1 — รันเดี่ยว ๆ ก่อน
alter type user_role add value if not exists 'viewer';
```

```sql
-- ขั้นที่ 2 — รัน patches.sql ส่วน G ที่เหลือ (G1–G5)
```

**ตรวจผล:**

```sql
select unnest(enum_range(null::user_role))::text as roles;   -- ต้องมี viewer
select column_name from information_schema.columns
 where table_schema='public' and table_name='audit_headers' and column_name='locked_at';
select tgname from pg_trigger
 where tgrelid='public.audit_details'::regclass and not tgisinternal;  -- ต้องมี trg_chk_locked, trg_log_details
select proname from pg_proc
 where pronamespace='public'::regnamespace and proname='mark_schedule_done';
```

**สถานะ: ✅ รันบน Supabase แล้วครบทุกขั้น (5 ส.ค. 2026)**

จัดขั้นตอนไว้ในไฟล์ `supabase/RUN_2026-08-05.sql` — 5 ขั้น พร้อมแผนย้อนกลับ

| ขั้น | ผล |
|---|---|
| 0 · เก็บภาพก่อนแก้ | ✓ |
| 1 · `alter type ... add value 'viewer'` (รันเดี่ยว) | ✓ |
| 2 · G1–G5 | ✓ `Success. No rows returned` |
| 3 · ตรวจผล 7 query | ✓ ผ่านทุกข้อ |
| 4 · smoke test เรียก RPC จริง | ✓ ได้ `ไม่ได้รับมอบหมายงานนี้` = ฟังก์ชันเรียกได้ + สิทธิ์ทำงาน |

> **ขั้นที่ 4 คือสิ่งที่ขาดไปเมื่อเช้า** — ยืนยันว่า RPC **เรียกได้จริง** ไม่ใช่แค่ `create` สำเร็จ
> SQL Editor รันเป็น `postgres` ไม่มี `auth.uid()` → การถูกปฏิเสธคือผลที่ถูกต้อง

## 15. 🔴 ต้องทดสอบจริงหลัง deploy (บทเรียนจากเช้านี้)

งานนี้แตะ **submit flow ที่ auditor ใช้ทุกวัน** — ต้องกดจริงไม่ใช่แค่อ่านโค้ด

1. login เป็น auditor → ตรวจ 5ส → submit → ต้องได้หน้าสรุป
2. เข้าประวัติ → เปิดผลที่เพิ่ง submit → **แก้คะแนนต้องไม่ได้** (ล็อกแล้ว)
3. ตรวจจากงานที่มอบหมาย → submit → **งานต้องเปลี่ยนเป็น "เสร็จสิ้น"** (RPC ใหม่)
4. login เป็น viewer → ต้องไม่เห็นปุ่มตรวจ · เข้า `/audit.html` ตรงต้องเด้งกลับ
5. login เป็น admin → แก้ผลที่ล็อกแล้วต้องได้

---

---

# ส่วนที่ 3 — 🐛 ผลทดสอบจริง: เจอบั๊ก role ถูกแช่ใน localStorage

## 16. ผลทดสอบ 4 เคส

| # | ทดสอบ | ผล |
|---|---|---|
| 1 | auditor ตรวจจากงานที่มอบหมาย → submit | ✅ ถูกต้อง (งานเปลี่ยนเป็น "เสร็จสิ้น" — RPC ใหม่ทำงาน) |
| 2 | auditor แก้ผลที่ล็อกแล้ว | ⚪ **ทดสอบผ่าน UI ไม่ได้** — ไม่มีปุ่มแก้คะแนน |
| 3 | viewer login | 🔴 **ยังเห็นปุ่มตรวจ + เข้าไปกรอกได้** |
| 4 | admin แก้ผลที่ล็อกแล้ว | ⚪ ทดสอบไม่ได้เหมือนข้อ 2 |

## 17. 🐛 Bug — `role` ถูกแช่ไว้ใน localStorage ตอน login

**อาการ:** เปลี่ยน role คนหนึ่งเป็น Viewer แล้ว เครื่องนั้นยังเห็นปุ่มตรวจ

**สาเหตุ:** `Session.save()` เขียน `{token, user}` ลง localStorage ตอน login
`Session.load()` อ่านกลับมาตรง ๆ · **ไม่มีจุดใดดึง role ใหม่จาก DB เลย**

```js
// เดิม — role มาจาก localStorage เท่านั้น
load() {
  const data = JSON.parse(localStorage.getItem(CONFIG.SESSION_KEY) || 'null');
  if (data) { AppState.token = data.token; AppState.user = data.user; return true; }
}
```

→ `isViewer()` อ่าน `AppState.user.role` = ค่าเก่า → guard ไม่ทำงาน
→ **admin เปลี่ยน role ให้คนที่กำลังล็อกอินอยู่ ไม่มีผลจนกว่าคนนั้นจะ logout**

**แก้ — เพิ่ม `Session.refreshRole()`** เรียกใน 5 จุด
(`initHome` · `initMyTasks` · `initPlant` · `initArea` · `initAudit`)

```js
async refreshRole() {
  const uid = (AppState.user || {}).userId;
  if (!uid) return null;
  const { data, error } = await _sb
    .from('profiles').select('role,status').eq('id', uid).single();
  if (error || !data) return null;

  // ถูกระงับการใช้งานระหว่างที่ยังล็อกอินอยู่ → เตะออก
  if (data.status && data.status !== 'active') {
    Session.clear(); navigate('index.html'); return null;
  }

  const fresh = MAP.role[data.role] || data.role;
  if (fresh !== AppState.user.role) {
    AppState.user.role = fresh;
    localStorage.setItem(CONFIG.SESSION_KEY,
      JSON.stringify({ token: AppState.token, user: AppState.user }));
  }
  return currentRole();
}
```

**ได้ของแถม:** ถ้า admin เปลี่ยน status เป็น `inactive` ระหว่างที่คนนั้นยังใช้งานอยู่
เดิม**ใช้งานต่อได้เรื่อย ๆ** ตอนนี้ถูกเตะออกทันทีที่เปลี่ยนหน้า

## 18. นโยบาย viewer — เลือก "เด้งกลับ" ไม่ใช่ "ซ่อนปุ่ม"

เดิมเขียนไว้ให้ `initHome` ซ่อนปุ่มตรวจ + bottom-nav → **เปลี่ยนแนวทาง**

| | ซ่อนปุ่ม | เด้งกลับ ✓ |
|---|---|---|
| หน้าหลัก | ดูโหว่ ปุ่มหลักหาย | เหมือน role อื่น |
| bottom-nav "ตรวจ" | ต้องซ่อนอีก **7 หน้า** | ไม่ต้องแตะเลย |
| หน้าใหม่ในอนาคต | ต้องไล่ซ่อนทุกครั้ง | ได้ฟรี |
| viewer เข้าใจว่าเกิดอะไร | ไม่รู้ว่าปุ่มหายไปไหน | เห็นข้อความบอกเหตุผล |

ทางเข้าการตรวจทุกทางมี guard อยู่แล้ว: `initMyTasks` · `initPlant` · `initArea` · `initAudit`

**🐛 เจอบั๊กซ้อน:** guard เดิมเขียน

```js
UI.toast(I18n.t('msg.viewer_no_audit'), 'error'); navigate('home.html');
```

`navigate()` เปลี่ยน `location.href` ทันที → **หน้าถูกทำลายก่อนคนอ่านทัน** toast กะพริบแล้วหาย

แก้ด้วย `bounceHome(msgKey)` — ฝาก key ไว้ใน `sessionStorage` แล้วให้ `initHome()` แสดงแทน

## 19. 🔑 ข้อค้นพบ: ระบบไม่มีฟีเจอร์แก้คะแนนย้อนหลัง

ทั้ง auditor และ admin **ไม่มีปุ่มแก้คะแนนใน UI เลย** ทำให้:

- การล็อก (G2) **ทดสอบผ่าน UI ไม่ได้** — แต่ยังมีค่าในฐานะด่านกัน API ตรง
  (คนที่มี anon key + audit_id ยิง `PATCH /audit_details` เข้ามาได้ ตอนนี้ยิงไม่เข้าแล้ว)
- ข้อ "admin แก้ผลที่ล็อกแล้วได้" ที่เขียนในแผน **ใช้ไม่ได้จริง** — admin ก็ไม่มีปุ่ม
  ถ้าต้องแก้ตอนนี้ต้องเข้า Supabase Dashboard

→ ถ้าจะให้ admin แก้ได้จริงต้องสร้าง UI ใหม่ (งานคนละก้อน — ยังไม่ทำ)

## 20. ✅ ทดสอบซ้ำหลัง deploy — ผ่านครบ

commit `1927f40` · deploy แล้ว · ทดสอบด้วยบัญชี viewer จริง

| ทดสอบ | ผล |
|---|---|
| หน้าหลัก → "เริ่มตรวจ 5ส" → เด้งกลับ + เห็นข้อความ | ✅ |
| bottom-nav "ตรวจ" → เด้งกลับ | ✅ |
| พิมพ์ `/audit.html` ตรง → เด้งกลับ | ✅ |
| dashboard / ประวัติ / มาตรฐาน ดูได้ปกติ | ✅ |
| เปลี่ยน role กลับเป็น Auditor ระหว่างล็อกอินอยู่ → ใช้ได้ทันทีไม่ต้อง logout | ✅ |

ข้อสุดท้ายคือตัวพิสูจน์ว่า `refreshRole()` ทำงานจริง ไม่ใช่แค่ compile ผ่าน

## 21. ช่องที่ยังเปิดอยู่ — viewer อัปโหลดรูปได้

`audit_photos_insert` เช็กแค่ `bucket_id` → viewer อัปโหลดไฟล์เข้า bucket ได้
ไม่กระทบผลตรวจ (G1 บล็อก header ไว้) แต่เปลืองพื้นที่

SQL พร้อมแล้วใน `RUN_2026-08-05.sql` **ขั้นที่ 5** — ยังไม่รัน

---

# ส่วนที่ 4 — 🔐 security ระยะที่ 1 + เลิกใช้ปุ่มลบผู้ใช้

## 22. ปุ่ม "ลบผู้ใช้" → "ระงับการใช้งาน"

**ปุ่มเดิมใช้ไม่ได้จริงมาตลอด** และไม่มีใครรู้เพราะไม่เคยมีใครกดสำเร็จ

`audit_logs.user_id` และ `audit_headers.auditor_id` อ้าง `profiles(id)` แบบ **ไม่มี `on delete`**
→ Postgres บล็อก · ทุกคนที่เคย login มีแถวใน `audit_logs` (`logEvent('LOGIN')`) **จึงติดแทบทุกคน**

ถ้าลบสำเร็จก็ยังพัง — ลบแค่ `profiles` ไม่ลบ `auth.users` → อีเมลยังถูกจอง
คนนั้น login ผ่าน auth ได้แต่เจอ `ไม่พบโปรไฟล์`

**ตัดสินใจ: เอาปุ่มลบออกจากแอป** แทนด้วยระงับ/เปิดใช้งาน

| | |
|---|---|
| `SBH.deleteUser()` | ลบออก → `SBH.setUserStatus({ userId, status })` |
| `deleteUser()` | ลบออก → `toggleUserStatus()` |
| `users.html` | ปุ่มแดง "ลบผู้ใช้นี้" → ปุ่มสลับสี (แดง=ระงับ / เขียว=เปิดใช้งาน) + คำอธิบายใต้ปุ่ม |
| ลบถาวรจริง | `supabase/delete_user.sql` (4 ขั้น ใส่อีเมลที่เดียว) |

**เหตุผลเชิงระบบ:** นี่คือระบบ audit — ลบคนออกทำให้ผลตรวจเก่าไม่มีเจ้าของ = ทำลายคุณค่าของ trail
ระงับได้ผลเหมือนกันทุกอย่างที่ต้องการ แต่เก็บประวัติไว้ครบ

**กันพลาด 2 ชั้น:** ระงับตัวเองไม่ได้ · ระงับ admin ที่ active คนสุดท้ายไม่ได้

**เรื่องที่ตรวจแล้วพบว่าทำไว้ดีอยู่แล้ว:** `getScheduleAdmin()` มี `.eq('status','active')`
→ คนที่ถูกระงับหลุดออกจากรายชื่อผู้ตรวจในหน้ามอบหมายเองอัตโนมัติ ไม่ต้องแก้

## 23. security ระยะที่ 1 — ทำครบส่วนที่เป็นโค้ด

| ข้อ | งาน | สถานะ |
|---|---|---|
| 1 | password policy 12 ตัว + leaked password protection | ⬜ Dashboard |
| 2 | JWT expiry 30 นาที + refresh rotation | ⬜ Dashboard |
| 3 | Storage file size limit + MIME allowlist | ⬜ Dashboard |
| 4 | `<meta>` CSP + `referrer` ทุกหน้า | ✅ 14 ไฟล์ |
| 5 | `escHtml()` เติม `'` | ✅ |
| 6 | ปิด `console.log` ใน production | ✅ |
| 7 | ลบอีเมล admin จาก SQL + ลบ `design-ref.html` | ✅ |
| 8 | ยืนยัน SQL ใน DB ตรงกับไฟล์ | ✅ ทำแล้ว 4 ส.ค. (19 policies ตรงทุกตัว) |

### ข้อ 4 — CSP ผ่าน `<meta>`

GitHub Pages ตั้ง HTTP header เองไม่ได้ จึงใช้ `<meta http-equiv>`
**ข้อจำกัดที่ต้องรู้:** `Report-Only` ใช้ใน meta ไม่ได้ → บังคับใช้ทันที ไม่มีโหมดทดลอง

```
default-src 'self';
script-src  'self' 'unsafe-inline' https://cdn.jsdelivr.net;
style-src   'self' 'unsafe-inline' https://cdn.jsdelivr.net https://fonts.googleapis.com;
font-src    'self' https://cdn.jsdelivr.net https://fonts.gstatic.com;
img-src     'self' data: blob: https://<project>.supabase.co;
connect-src 'self' https://<project>.supabase.co wss://<project>.supabase.co;
base-uri 'self'; form-action 'self'; object-src 'none'
```

**ตรวจก่อนใช้ — เขียนสคริปต์ไล่ทุก external resource เทียบกับ policy:**

| resource | directive | ผล |
|---|---|---|
| `cdn.jsdelivr.net` (supabase-js, bootstrap-icons) | script-src, style-src, font-src | ✓ |
| `fonts.googleapis.com` (CSS) | style-src | ✓ |
| `fonts.gstatic.com` (ไฟล์ฟอนต์ที่ `@font-face` ไปดึงต่อ) | font-src | ✓ |
| `<project>.supabase.co` (REST + Storage + Realtime) | connect-src, img-src | ✓ |
| `data:` / `blob:` (รูปจากกล้อง + ที่ย่อขนาดแล้ว) | img-src | ✓ |
| `manifest.json` · `sw.js` | fallback → `'self'` | ✓ |

> ⚠️ **False positive ที่เกือบทำให้แก้ผิด:** สคริปต์รายงานว่า `http://www.w3.org` ถูกบล็อก
> ตรวจแล้วเป็น `xmlns="http://www.w3.org/2000/svg"` — namespace ของ SVG **ไม่ใช่ network request**
> CSP ไม่เกี่ยวเลย · ถ้าเชื่อผลสคริปต์ดิบ ๆ จะเผลอเพิ่ม w3.org เข้า connect-src โดยไม่จำเป็น

`'unsafe-inline'` ยังต้องมี เพราะทุกหน้าใช้ inline `onclick` + `<style>` การถอดออกคือข้อ 24 ของระยะที่ 3

**ย้อนกลับ:** ลบ 2 บรรทัด meta ออกจากหน้าที่มีปัญหา (เขียนคอมเมนต์เตือนไว้เหนือ meta แล้ว)

### ข้อ 6 — ปิด `console.log`

```js
(function () {
  const isLocal = /^(localhost|127\.0\.0\.1|\[::1\])$/.test(location.hostname);
  let debugOn = false;
  try { debugOn = localStorage.getItem('5s_debug') === '1'; } catch (_) {}
  if (!isLocal && !debugOn) {
    const noop = function () {};
    console.log = noop; console.debug = noop; console.info = noop;
  }
})();
```

คง `warn` / `error` ไว้ — จำเป็นตอนตามปัญหาจริง
**ประตูหลังสำหรับดีบั๊ก:** `localStorage.setItem('5s_debug','1')` แล้ว refresh → log กลับมา
(จำเป็นเพราะเพิ่งเพิ่ม `[Session] role เปลี่ยน` ไว้ตามบั๊กเมื่อกี้)

### ข้อ 7 — ลบ `design-ref.html`

ไฟล์ 939 KB ไม่มีอะไรอ้างถึงเลย · เป็นไฟล์อ้างอิงดีไซน์ที่หลุดเข้า repo
ข้างในมี CDN แปลกปลอม (`unpkg.com/react`) ที่ไม่เกี่ยวกับระบบ

`storage_and_first_admin.sql` — เปลี่ยนอีเมลจริงเป็น `REPLACE_WITH_ADMIN_EMAIL`
+ เขียนเตือนไม่ให้ commit อีเมลจริงลง repo public

## 24. ต้องรันเพิ่มบน Supabase

```sql
drop policy if exists audit_photos_insert on storage.objects;
create policy audit_photos_insert on storage.objects
  for insert to authenticated
  with check (
    bucket_id = 'audit-photos'
    and coalesce(public.auth_role()::text, '') <> 'viewer'
  );
```

อัปเดตทั้งใน `storage_and_first_admin.sql` (DB ใหม่) และ `RUN_2026-08-05.sql` ขั้นที่ 5

---

# ส่วนที่ 5 — 👥 ผู้ตรวจหลายคนต่อพื้นที่ (สถานะรายคน) — "ส่วน H"

## 25. ปัญหา

`schedules` 1 แถว = 1 พื้นที่ มี `auditor_ids uuid[]` (หลายคน) แต่ **`status` ช่องเดียว**

คนแรก submit → `mark_schedule_done()` เขียน `status='completed'` → **ทั้งแถวเสร็จสำหรับทุกคน**

**อาการที่มาจากสาเหตุเดียวกัน 4 ตัว** (เจอ 3 ตัวจากการอ่านโค้ด ผู้ใช้แจ้งมาแค่ตัวแรก):

| # | อาการ |
|---|---|
| 1 | คนที่ 2 เห็น "เสร็จสิ้น" → ปุ่มเปลี่ยนเป็น "ดูผล" → ตรวจไม่ได้ |
| 2 | 🐛 ปุ่ม "ดูผล" **เปิดผลของคนอื่น** — `getSchedule()` จับคู่จาก `plant+area` เอาอันล่าสุด ไม่กรอง `auditor_id` |
| 3 | 🐛 KPI ตารางตรวจนับ "แถว" → 2 คน คนแรกเสร็จ = **1/1 = 100%** |
| 4 | 🐛 ความก้าวหน้ารายคนผิด — คนที่ 2 ขึ้นว่าเสร็จแต่ไม่มีคะแนน (ที่มาของคำถาม "คุณอ้น 94% คุณ... 88% คืออะไร") |

## 26. การตัดสินใจ 5 ข้อ

| # | เลือก |
|---|---|
| 1 | Dashboard เฉลี่ย **รายคน** `avg(percent)` ไม่ใช่ pooled |
| 2 | จัดกลุ่ม `plant + area` · เลือกรอบการตรวจได้ |
| 3 | คงนโยบาย 4 ส.ค. — auditor เห็น "เสร็จแล้ว" ของเพื่อน **ไม่เห็น %** · admin เห็นครบ |
| 4 | 1 คนตรวจ 1 งานได้ครั้งเดียว |
| 5 | คนที่ถูกระงับ **ไม่นับ** เป็น "ต้องตรวจ" |

**D1** เพิ่มปุ่ม admin ลบผลตรวจในงานนี้ · **D2** (กระดาน admin รองรับหลายรอบ) แยกเป็นงานถัดไป

## 27. สถาปัตยกรรม

```
audit_headers  + schedule_id uuid  FK on delete set null
               + audit_round text  ← ก๊อปจาก schedule ตอน insert
               + unique (schedule_id, auditor_id) where schedule_id is not null

trigger  trg_chk_header_schedule    BEFORE INSERT       validate + ก๊อปรอบ
trigger  trg_sync_sched_status      AFTER INS/DEL       sync schedules.status
trigger  trg_sync_sched_status_upd  AFTER UPD + WHEN    ← ดูข้อ 29
view     schedule_progress          security_invoker    ความจริงเรื่อง "ใครเสร็จ"

ลบ  mark_schedule_done() RPC + SBH.completeSchedule()   ← ใช้ได้แค่วันเดียว
```

**นิยาม:** *auditor X ทำงาน S เสร็จ ⟺ มี `audit_headers` ที่ `schedule_id=S`, `auditor_id=X` และ finalize แล้ว*
ไม่เก็บสถานะรายคนไว้ที่ไหน → ข้อมูลขัดกันเองไม่ได้

## 28. 🔑 ทำไมใช้ view ไม่คำนวณใน JS

แผนแรกให้ `getSchedule` · `getAssignmentAnalytics` · `getScheduleAdmin` **แต่ละตัวคำนวณเอง**

> นี่คือบั๊กคลาสเดียวกับ **"pill 94% แต่ badge 100%"** ที่เจอเมื่อ 4 ส.ค. — เลขเดียวกัน
> คำนวณ 2 ที่ด้วยขอบเขตต่างกันแล้วขัดกันเอง คราวนี้จะเป็น **3 ที่**

จึงนิยามครั้งเดียวใน DB เป็น view ทุกหน้าอ่านจากที่เดียวกัน

⚠️ **`with (security_invoker = true)` จำเป็นมาก** — ถ้าไม่ใส่ view จะรันด้วยสิทธิ์เจ้าของ
→ RLS ของ `schedules` ไม่ทำงาน → **auditor เห็นงานของทุกคน** (ย้อนนโยบาย 4 ส.ค.)

## 29. 🐛 บั๊กที่เจอตอนตรวจโค้ดตัวเอง — 5 ตัว

### 29.1 trigger ยิง ~67 ครั้งต่อการ submit 1 ครั้ง

`trg_recalc_audit` บน `audit_details` เป็น **`for each row`** → เกณฑ์ 67 ข้อ = `audit_headers`
ถูก UPDATE 67 ครั้ง ถ้า sync trigger ไม่มีเงื่อนไข จะอ่าน view (join 3 ตาราง) 67 รอบ

**แก้** แยก trigger ของ UPDATE ออกมาใส่ `WHEN`:

```sql
create trigger trg_sync_sched_status_upd
after update of locked_at, status on public.audit_headers
for each row
when (old.status is distinct from new.status
   or old.locked_at is distinct from new.locked_at)
execute function public.sync_schedule_status();
```

### 29.2 log ขยะ 1 แถวต่อการ submit

`trg_log_schedules` เป็น `after insert or update or delete` → ถ้า sync เขียน `status` ทุกครั้ง
จะได้ log ขยะ (ปัญหาเดียวกับที่ `trg_log_headers` ตั้งใจเลี่ยงไว้)

**แก้** เขียนเฉพาะตอนค่าเปลี่ยนจริง — `and status is distinct from (...)` → log ที่ได้คือ "งานปิดแล้ว" ซึ่งมีประโยชน์

### 29.3 header ค้างทำให้ auditor ตันสนิท

flow คือ `insert header` → ส่ง details เป็นชุด → `finalize` ถ้าเน็ตหลุดกลางทางและ rollback
ไม่สำเร็จ จะเหลือ header ที่ยัง `pending` → **unique index บล็อกการตรวจใหม่ ตันถาวร**

**แก้** `submitAuditHeader` ลบ header ที่ยังไม่ finalize (`locked_at is null` + `pending`) ของตัวเองก่อน (self-heal)
และ view นับว่า "เสร็จ" เฉพาะ `locked_at is not null or status <> 'pending'`

### 29.4 กรอกครบ 67 ข้อแล้วค่อยถูกปฏิเสธ

`getAreas()` แนบ `Schedule_ID` ให้พื้นที่ที่ schedule ยัง `pending` — ซึ่งยัง pending อยู่จนทีมครบ
→ **คนที่ตรวจไปแล้วเลือกพื้นที่นั้นเองได้อีก** กรอกครบ + อัปโหลดรูป (STEP 0) แล้วเพิ่งโดน
unique ปฏิเสธที่ STEP 1 → เสียแรงเปล่า + รูปกำพร้าใน Storage

**แก้** เพิ่ม `SBH.hasAuditedSchedule()` เช็กที่ต้นทาง `initAudit()` → เด้งไปหน้าผลเดิมพร้อมข้อความ

### 29.5 `_profileCache` ค้างทำให้สิทธิ์รั่ว

`Session.refreshRole()` (ทำเมื่อบ่าย) อัปเดต `AppState` แต่ **ไม่ล้าง `_profileCache`**
ซึ่ง `getAssignmentAnalytics()` ใช้ตัดสิน `isStaff` → คนที่ถูกลดจาก admin กลางเซสชัน
**ยังเห็น % ของทุกคน** → เพิ่ม `_profileCache = null` ตอน role เปลี่ยน

## 30. ปิดช่องโหว่ที่การเปลี่ยนนี้สร้างขึ้น

`schedule_id` มาจาก client (`getParam('scheduleId')`) → เชื่อไม่ได้
ถ้าไม่ตรวจ auditor จะใส่ `schedule_id` ของงานคนอื่นแล้ว**ปิดงานคนอื่น**ได้ (BOPLA ตัวใหม่)

```sql
if not (new.auditor_id = any(v_ids)) then
  raise exception 'คุณไม่ได้รับมอบหมายงานนี้';
end if;
new.audit_round := v_round;   -- ก๊อปไว้ ไม่ join ตอนอ่าน
```

**ทำไมก๊อป `audit_round` ไม่ join:** `on delete set null` → ลบงานที่มอบหมายแล้วผลตรวจจะไม่รู้ว่ารอบไหน
และถ้า admin แก้ `audit_round` ทีหลัง ประวัติต้องไม่เปลี่ยนย้อนหลัง

## 31. ทำไมเฉลี่ยรายคนถูกกว่า pooled

`na` (ไม่มีในพื้นที่) ทำให้ `max_score` ของแต่ละคนไม่เท่ากัน

| พื้นที่ A | อ้น 90% (เต็ม 100) · สมชาย 80% (เต็ม 50) |
|---|---|
| pooled (เดิม) | (90+40)/150 = **86.7%** ← อ้นมีน้ำหนัก 2 เท่า |
| เฉลี่ยรายคน ✓ | (90+80)/2 = **85%** ← ทุกคนเท่ากัน |

**ป้ายชื่อ Area Ranking เพิ่มชื่อโรงงาน** — `area_id` ไม่ชนกัน (`SUP-WH-F1`) แต่ **ชื่อซ้ำ**
("Warehouse F1" มีทั้ง 3 โรงงาน) เดิม ranking มี 3 แถวชื่อเหมือนกันแยกไม่ออก

## 32. ทดสอบ logic ด้วยสคริปต์ — 26/26 ผ่าน

จำลอง logic ของ view + KPI + Dashboard + `getSchedule` + `_schedStatus` ใน Node
รวม edge case: array ว่าง · หารศูนย์ · N/A ทั้งหมด · header ค้าง · ระงับคนกลางงาน ·
admin ถอดคนที่ตรวจแล้วออก (ต้องไม่เกิน 100%) · มอบหมายแต่ inactive หมด · รอบ 1/รอบ 2 ไม่ปนกัน

ตรวจอัตโนมัติเพิ่ม: i18n 199 key ครบ 2 ภาษา · HTML tag สมดุล · id ที่ JS อ้างมีจริง ·
ฟังก์ชันที่ HTML เรียกมีจริง · ไม่มีโค้ดตายอ้างของที่ลบไป · `$$` สมดุลทุกไฟล์ SQL

## 33. D1 — ปุ่ม admin ลบผลตรวจ

จำเป็นเพราะ `locked_at` + `unique(schedule_id, auditor_id)` รวมกัน = **ตรวจผิดแล้วแก้ไม่ได้ ตรวจใหม่ก็ไม่ได้**

วางที่ `summary.html` (ไม่ใช่ลิสต์ในหน้าประวัติ — กดพลาดยาก) · ยืนยันด้วยรายละเอียดจริง
(พื้นที่ · วันที่ · คะแนน · จำนวนข้อ · จำนวนรูป) · **ลบรูปใน Storage ด้วย** โดยอ่าน `photo_urls`
ก่อนลบแถว ไม่งั้นเหลือไฟล์กำพร้า (บทเรียนเดียวกับ `admin_reset_data`)

RLS รองรับอยู่แล้วจาก G2 เมื่อเช้า — ไม่ต้องเพิ่ม policy · ลบแล้ว trigger เปิดงานกลับเป็นค้างเอง

> **หมายเหตุ:** ลบ 1 ใบได้ log ~67 แถวจาก `trg_log_details` — **ถือเป็นข้อดี**
> มี `old_data` ครบทุกข้อไว้กู้คืนถ้าลบผิด

## 34. ⚠️ ข้อจำกัดระหว่างที่ D2 ยังไม่แก้

`getScheduleAdmin()` ยังเป็น `byArea[s.area_id] = s` → **1 พื้นที่เห็นได้แค่ 1 งาน**

> ⛔ อย่ามอบหมายพื้นที่เดียวกันซ้อน 2 รอบพร้อมกัน — กระดาน admin จะเห็นแค่รอบเดียว
> ✅ ปิดรอบเดิมให้ครบก่อนแล้วค่อยมอบหมายรอบใหม่

Dashboard/ตารางตรวจ **ไม่กระทบ** เพราะอ่านจาก `audit_headers` ที่มี `audit_round` ติดมาแล้ว

**อีกจุดที่เจอแต่ยังไม่แก้:** `getAreas()` มี `['admin','manager'].includes(prof.role)` ค้างอยู่
ไม่ตรงกับ G5 (`is_staff()` = admin) — ไม่กระทบเพราะไม่มีใครเป็น manager แล้ว

## 35. สถานะ deploy ส่วน H

| | |
|---|---|
| SQL ส่วน H รันบน Supabase | ✅ ครบ (ผ่านทั้ง A–E + view อ่านได้) |
| commit + push | ✅ `958e70a` (จาก `4a6ebd9`) — 20 ไฟล์ |
| `RUN_2026-08-05_H.sql` | ไฟล์คู่มือรัน (ก๊อปทั้งไฟล์ครั้งเดียว + ตรวจผล + แผนย้อนกลับ) |
| ทดสอบจริง 9 เคส | ⏳ **ยังไม่ได้ทดสอบ** — เปิดเซสชันใหม่แล้วเริ่มที่เคส 1 |

### 🔴 ทดสอบจริงหลัง deploy — ยังค้าง (บทเรียน "โค้ดมีอยู่ ≠ ทำงานได้")

Cmd+Shift+R ก่อน (`v=41` / `sw v5.5`) · เปิด DevTools Console ค้างตอน submit

| # | ทำ | ควรได้ |
|---|---|---|
| 1 ⚠️ | มอบพื้นที่ A ให้ 2 คน → คนที่ 1 submit | ตารางตรวจ **1/2 = 50%** · กระดาน admin badge "บางส่วน" |
| 2 | คนที่ 2 เปิด mytasks | เห็น "เริ่มตรวจ" ไม่ใช่ "ดูผล" |
| 3 | คนที่ 2 submit | งานปิด · **2/2 = 100%** |
| 4 | คนที่ 1 กด "ดูผล" | เห็นผล**ของตัวเอง** ไม่ใช่คนที่ 2 |
| 5 | คนที่ 1 ตรวจพื้นที่ A ซ้ำ | เด้งกลับ **ก่อน**กรอก |
| 6 | auditor เปิดตารางตรวจ | เพื่อน "เสร็จแล้ว" ไม่เห็น % (ดู Network tab) |
| 7 | admin เปิด summary → ลบผลตรวจ | ลบได้ · รูปหาย · งานกลับเป็นค้าง |
| 8 | Dashboard เลือกรอบ | เฉลี่ยรายคน · Area Ranking มีชื่อโรงงานนำหน้า |
| 9 | admin ระงับคนที่ 2 ตอนงานค้าง | งานปิดเอง (1/1) |

> **เคส 1 เสี่ยงสุด** — ถ้า `trg_chk_header_schedule` ผิดจะบล็อก insert = submit ไม่ได้ทั้งระบบ
> ถ้าพัง ย้อนด่วนด้วยคำสั่งท้าย `RUN_2026-08-05_H.sql`
> **เคส 6 ดูด้วยตาไม่ออก** — ต้องเปิด Network tab ดู response `audit_headers` ว่าไม่มี % คนอื่น

---

# ส่วนที่ 6 — 🔧 แก้ enum cast + รื้อ "พื้นที่ต้องปรับปรุง"

## 36. 🐛 sync_schedule_status() — cast enum ที่ SET (ต่อจากส่วน H)

ทดสอบเคส "มอบ 1 พื้นที่ 3 auditor แล้ว submit ไม่ได้" → error
`column "status" is of type schedule_status but expression is of type text`

`set status = case ... end` คืน text แต่ column เป็น enum — ผมใส่ `::schedule_status`
แค่ที่เงื่อนไข `where` ลืมใส่ที่ `set` (A2 recalc ทำถูกด้วย `::audit_status`)

แก้: `set status = (case ... end)::schedule_status` ทั้ง `patches.sql` + `RUN_2026-08-05_H.sql`
รันซ้ำเฉพาะฟังก์ชันเดียว (`create or replace`) บน Supabase แล้ว ✅

> sim ใน Node จับ logic ผิดได้ แต่จับ **ชนิดข้อมูล SQL ไม่ได้** — ต้องรันจริงถึงเจอ (ซ้ำรอย admin_reset)

## 37. รื้อ "พื้นที่ต้องปรับปรุง" — เห็นเลยไม่ต้องหา

เดิม 3 dropdown (โรงงาน → พื้นที่ → ครั้งตรวจ) = ต้องรู้ล่วงหน้าว่าจะดูอะไรแล้วเจาะเข้าไป

ใหม่:
- **รอบ:** ใช้ dropdown เดียวกับ Ranking (`dashRound`) → เปลี่ยนที่เดียวขยับทั้งหน้า
- **เปิดมาเห็นเลย:** ข้อที่ตก (0-1) ของทุกพื้นที่ในรอบนั้น เรียง 0 ก่อน แล้ว 1 · ใหม่ก่อน
- **area dropdown:** ตัวกรองเสริมตัวเดียว ('' = ทุกพื้นที่)
- **แต่ละข้อ = accordion:** แตะแถวกางดู comment + รูปย่อ · แตะรูปซูมเต็มจอ (lightbox)
- ตัวนับ 0/1 ด้านบน

ทำได้เพราะ ส่วน H — `audit_headers` มี `audit_round` แล้ว จึงกรองข้อที่ตกตามรอบได้

**โค้ด:**
- `SBH.getImprovementItems({round})` แทน `getAuditedAreas` + `getAreaAudits` (ลบทิ้ง)
  ยิง header(รอบ) → details(`score<=1`, `na=false`) → join criteria → maps · **ได้เฉพาะ 0/1**
- client รื้อ `imp*` ทั้งชุด → `impFillAreaFilter` · `impAreaChange` · `impRenderFeed` ·
  `impToggle` · `impZoom` / `impCloseZoom`
- `dashboard.html`: เหลือ area dropdown เดียว + lightbox container + CSS accordion/lightbox
- ตกลง: โชว์ **0 และ 1 เท่านั้น** (ตัด N/A และ 2 ออก)

sim logic 7/7 ผ่าน (เรียง · กรอง · นับ · dropdown ไม่ซ้ำ) · i18n 195 key ครบ · bump v=42 / sw v5.6

**ยังไม่ทดสอบจริง** — ต้องกดดูบนมือถือว่า accordion กาง/หุบลื่น + lightbox เปิดรูปได้

---

## สิ่งที่ต้องทำต่อ

### ✅ ปิดไปแล้ววันนี้

- เคลียร์สิทธิ์ → 3 roles · ปิด BOPLA (`headers_insert` + `schedules`) · ล็อกผลตรวจ · log การแก้คะแนน (ส่วนที่ 2)
- security ระยะที่ 1 **ส่วนโค้ด** — CSP + referrer 14 หน้า · `escHtml` เติม `'` · ปิด `console.log` · ลบ `design-ref.html` + email admin (ส่วนที่ 4)
- กัน viewer อัปโหลดรูป (storage policy)
- ปุ่มลบผู้ใช้ → ระงับ/เปิดใช้งาน + `delete_user.sql` (ส่วนที่ 4)
- ผู้ตรวจหลายคนต่อพื้นที่ + `audit_round` ใน `audit_headers` + ปุ่ม admin ลบผลตรวจ (ส่วนที่ 5)

### 🟠 ค้างอยู่ — เรียงตามความคุ้มค่า

1. **ทดสอบจริง 9 เคสของส่วน H** (ดูข้อ 35) — ก่อนอื่นเลย
2. **security ระยะที่ 1 — 3 ข้อที่ต้องทำใน Supabase Dashboard**
   - password policy 12 ตัว + leaked password protection (ต้อง Pro)
   - JWT expiry 1800s (refresh rotation เปิดอยู่แล้ว) + inactivity timeout (ต้อง Pro)
   - Storage `audit-photos`: file size limit 2 MB + MIME `image/jpeg`
   - หมายเหตุ: ค้นเจอว่า "ลด JWT expiry" ได้ผลน้อยกว่าคิด — refresh token อยู่ localStorage
     ไม่มีวันหมดอายุ · ตัวจริงคือ inactivity timeout (Pro) หรือระยะที่ 2 ข้อ 9 (เลิกเก็บ token เอง)
3. **D2 — กระดาน admin รองรับหลายรอบต่อพื้นที่** (แยกจากส่วน H)
   `getScheduleAdmin()` ยังเป็น `byArea[s.area_id] = s` → 1 พื้นที่เห็นแค่ 1 งาน
   ระหว่างนี้: ปิดรอบเดิมให้ครบก่อนมอบหมายรอบใหม่

### 🟡 งานเก็บตก

4. **สำรองรูปก่อนลบ** — Storage API `copy()` ไป bucket `audit-photos-backup`
5. `getAreas()` แก้ `['admin','manager']` ให้ตรงกับ G5 (`is_staff()` = admin เท่านั้น)
6. ลบ dead code `en:` ซ้ำใน `app.js` · แก้ `<strong>` หายใน `home.html` 3 จุด ·
   เพิ่ม `assign.html`/`logs.html` ใน `STATIC_ASSETS` ของ `sw.js`
7. UI ให้ admin แก้คะแนนย้อนหลัง (ตอนนี้มีแค่ลบทั้งใบแล้วตรวจใหม่)
