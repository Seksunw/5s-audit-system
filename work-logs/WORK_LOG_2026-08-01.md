# สรุปงาน — 5S Audit System

**วันที่:** 1 สิงหาคม 2026
**Repo:** `Seksunw/5s-audit-system` (branch `main`)
**Live:** https://seksunw.github.io/5s-audit-system/

---

## หัวข้อหลัก: Flow การมอบหมายงาน (admin) + การตรวจงานที่ได้รับมอบหมาย (auditor)

## 1. แก้บั๊ก: ปุ่ม "เริ่มตรวจ" ในงานที่มอบหมายใช้ไม่ได้

**อาการ:** auditor กด "เริ่มตรวจ" จากการ์ดงานของฉัน แล้วเด้งกลับหน้าเลือกโรงงาน

**สาเหตุ:** `startAssignedAudit` สั่งไป `audit.html` โดยไม่ส่ง URL params แต่ `initAudit`
อ่านค่าจาก `getParam(...)` เมื่อไม่เจอ plantId/areaId → เด้งกลับ plant.html

**แก้:** ส่ง params ครบ (plantId, plantName, areaId, areaName, areaType, scheduleId)
→ auditor กดแล้วเข้าตรวจพื้นที่ที่มอบหมายทันที ไม่ต้องเลือกพื้นที่เอง

## 2. หลังตรวจงานที่มอบหมายเสร็จ → mark เป็น "เสร็จสิ้น"

- หลัง submit สำเร็จ ถ้ามาจากงานที่มอบหมาย (มี scheduleId) → เรียก `completeSchedule`
  set schedule เป็น `completed` (ไม่ให้ล้มทั้ง flow ถ้าพลาด)
- การ์ดงานยังแสดงอยู่ (ไม่หาย) แต่เปลี่ยนสถานะ:
  - Badge "📅 รอตรวจ" → "✓ เสร็จสิ้น" (สีเขียว)
  - ปุ่ม "เริ่มตรวจ" → ข้อความ "✓ ดำเนินการตรวจเสร็จสิ้นแล้ว" (ไม่มีปุ่ม)
  - การ์ดจางลงเล็กน้อย คงสไตล์ UI เดิม

## 3. คงการเข้าตรวจได้ทั้ง 2 ทาง

- ผ่านงานที่มอบหมาย (ตรงพื้นที่ทันที)
- เลือกพื้นที่เอง (plant → area) แบบเดิม

## เบื้องหลัง

- `getSchedule` คืน `Area_Type` + `Plant_Name` เพิ่ม และรวมงาน `completed` มาแสดงด้วย
  (join `areas(area_name, area_type)` + `plants(plant_name)`, filter status in pending/completed)
- เพิ่ม handler `completeSchedule({ scheduleId })`
- RLS: เพิ่ม policy `schedules_auditor_update` ให้ auditor ที่อยู่ใน `auditor_ids`
  อัปเดตสถานะงานของตัวเองได้ (เดิมมีแค่ staff)

---

## Migration ที่ต้องรันบน Supabase (รันแล้ว)

| ไฟล์ | ทำอะไร |
|---|---|
| `2026-08-01_schedule_auditor_complete.sql` | RLS ให้ auditor mark schedule ของตัวเองเป็น completed ได้ |

## เวอร์ชันไฟล์

- `app.js` / `style.css` → `v=17`
- service worker cache → `v3.0`

## Commit หลัก

- `feat: auditor กดงานที่มอบหมายเข้าตรวจตรงพื้นที่ + mark เสร็จสิ้นหลัง submit (แก้บั๊กปุ่มเริ่มตรวจ)` (`4bc88a1`)

## 4. จัดระเบียบเอกสาร + migration

- ย้าย WORK_LOG ทั้งหมดเข้าโฟลเดอร์ `work-logs/` (`ce6ab6d`)
- **รวม migration ทั้งหมดเป็นไฟล์เดียว** `supabase/patches.sql` แล้วลบไฟล์ราย patch (5 ไฟล์) (`8717d68`)
  - แบ่งเป็น ส่วน A (โครงสร้าง — รันซ้ำปลอดภัย) และ ส่วน B (ข้อมูล — ตั้งครั้งเดียว)
  - เขียน idempotent ทั้งไฟล์ รันซ้ำได้ไม่พัง
  - ต่อไปมีแก้ DB → เพิ่มต่อท้าย `patches.sql` ไฟล์เดียว ไม่สร้างไฟล์ใหม่
- ล้าง saved queries ใน Supabase SQL Editor เหลือ 4 อันตรงกับไฟล์ใน repo
  (`01_schema`, `02_seed_master`, `03_storage_roles`, `patches`)

### โครงสร้างไฟล์ SQL (สรุป)

| ไฟล์ | หน้าที่ | รันเมื่อไหร่ |
|---|---|---|
| `supabase/schema.sql` | โครงสร้างตัวจริง | **เฉพาะ DB ใหม่เปล่า** (ห้ามรันบน DB ที่ใช้อยู่ → error already exists) |
| `supabase/seed_master.sql` | ข้อมูลตั้งต้น (plants, areas, criteria) | ครั้งเดียวตอนตั้ง DB ใหม่ |
| `supabase/patches.sql` | รวมการแก้ DB สะสม (ไฟล์เดียว) | **DB ที่ใช้อยู่** เวลามีแก้เพิ่ม (รันซ้ำได้) |
| `supabase/storage_and_first_admin.sql` | storage bucket + admin คนแรก | ครั้งเดียว |

## 5. หน้าตารางตรวจ (Assignment Analytics) — `assign.html`

หน้าใหม่ให้ admin/auditor ดูภาพรวมการมอบหมาย (`2c38605`)

- KPI: มอบทั้งหมด / เสร็จ / ค้าง / เกินกำหนด + แถบความคืบหน้า
- ตารางรายผู้ตรวจ: จำนวนพื้นที่, เสร็จ/ค้าง, **คะแนนเฉลี่ยที่ให้** (ชี้ความเข้มงวด) — แตะชื่อกางดูพื้นที่ทั้งหมด
- ตัวกรอง รอบ + โรงงาน
- RLS จำกัดขอบเขต: admin เห็นทุกคน / auditor เห็นเฉพาะตัวเอง
- ปุ่ม "ตารางตรวจ" ในเมนูด่วน home (ทั้ง admin + auditor)

## 6. ปรับหน้ามอบหมาย (schedule) ให้ใช้ง่ายขึ้น

รื้อ flow เดิมที่ต้องมอบทีละพื้นที่ (`23b660c`, `d6a6d38`, `5243a4d`)

- **เลือกหลายพื้นที่พร้อมกัน (bulk)** — ติ๊กการ์ด → มอบทีเดียว
- **โหมด "ตามพื้นที่" (SET)** — เลือกผู้ตรวจ(หลายคน)+วัน+รอบ ทับให้พื้นที่ที่เลือก
- **โหมด "ตามคน" (ADD)** — เลือกผู้ตรวจหลายคน → ติ๊กพื้นที่ → เพิ่มแบบ union (คนเดิมยังอยู่)
- **แถบวางแผน** ตั้งรอบ+วันครบกำหนดครั้งเดียว ใช้เป็น default
- **ตัวกรอง** ยังไม่มอบ/เกินกำหนด + **ค้นหา** ชื่อพื้นที่
- แก้บั๊ก: nav ทับกล่อง KPI, เช็คบ็อกซ์ทับป้ายสถานะ
- Logic 1 พื้นที่-หลายคน / 1 คน-หลายพื้นที่ คงเดิม (ไม่แตะ DB)

## 7. ระบบ Audit Log (ความปลอดภัยหลังบ้าน)

เก็บกิจกรรมทั้งหมดของแอปลง Supabase (`8fd54b8`)

- **DB trigger `log_activity()`** ติด 5 ตาราง เก็บ INSERT/UPDATE/DELETE พร้อมค่าเดิม→ใหม่ (JSON)
  อัตโนมัติ ไคลเอนต์ bypass ไม่ได้
  - `profiles`, `schedules`, `areas`, `criteria` — ครบ I/U/D
  - `audit_headers` — เฉพาะสร้าง/ลบ (เลี่ยง noise จาก trigger คำนวณคะแนน)
- **Client log** — LOGIN, LOGOUT, SUBMIT_AUDIT (พร้อมพื้นที่+คะแนน)
- **Append-only** — `revoke update, delete` → log แก้/ลบไม่ได้แม้แต่ admin, อ่านเฉพาะ admin (RLS)
- ขยายตาราง `audit_logs`: เพิ่ม entity, entity_id, old_data, new_data
- **หน้าดู log** `logs.html` (admin) — ตัวกรอง + แตะดูค่าเดิม→ใหม่ + ปุ่มเมนูด่วน admin-only

## 8. ปุ่มรีเซ็ตข้อมูล + สคริปต์ล้างข้อมูล (เตรียมไป production)

สำหรับล้างข้อมูลทดสอบก่อนเริ่มใช้จริง (`0dc722f`)

- **ปุ่มในแอป** — หน้าผู้ใช้ (users.html) → "เขตอันตราย" (แสดงเฉพาะ admin)
  - ด่านยืนยัน 3 ชั้น: พิมพ์ `RESET` + ใส่รหัสผ่าน (verify ด้วย Supabase auth) + popup โชว์ขอบเขต
  - RPC `admin_reset_data()` (security definer) — เช็ค admin ที่ DB, client ลบเองไม่ได้
  - **สำรองอัตโนมัติ** ลงตาราง `*_backup` ก่อนลบ, บันทึกการรีเซ็ตลง audit_logs
- **ขอบเขตลบ:** ประวัติ+คะแนน (audit_headers/details), การมอบหมาย (schedules), รูปใน Storage
- **เก็บไว้:** ผู้ใช้, audit log, ข้อมูลตั้งต้น
- ไฟล์ `supabase/reset_test_data.sql` (รันมือ) — ทางเลือกสำรอง มีบล็อกสำรอง/กู้คืน
- ⚠️ สร้างไว้พร้อม แต่ **ยังไม่ได้กดรีเซ็ต** — ข้อมูลทดสอบยังอยู่ครบ

---

## หัวข้อรอบบ่าย: ปรับ UI หน้า Home/งานมอบหมาย + ตรวจทั้งระบบ

## 9. รื้อหน้า Home เป็น Hero Card + สร้างหน้า "งานที่ได้รับมอบหมาย"

- **Home hero card** (`ed19bce`) — รวมทักทาย + รอบ/ครบกำหนด + สรุปงานค้าง + ปุ่ม "เริ่มตรวจ 5ส"
  เป็นการ์ดเดียว, ย้าย avatar ขึ้น top-nav, ตัดการ์ด "กำหนดการตรวจถัดไป" เดิมออก
  - `initHome` เติม `heroMeta`/`heroDesc` จากงานที่มอบหมาย (ไม่มีงาน → ข้อความพร้อมเริ่มตรวจ)
  - เพิ่มคีย์ i18n `home.greeting_hi` ครบ 3 ภาษา
- **หน้าใหม่ `mytasks.html`** (`7ecd48f`) — ปุ่ม "เริ่มตรวจ" บน Home พามาหน้านี้แทนหน้าเลือกพื้นที่
  - ย้าย section "งานที่ได้รับมอบหมาย" ออกจาก Home มาไว้หน้านี้
  - `initMyTasks` + router case `mytasks` + เพิ่มใน sw precache
  - ไม่มีงาน → empty state + ปุ่ม "เลือกพื้นที่ตรวจเอง"

## 10. ปรับรายการงานเป็นแบบกะทัดรัด + icon inline SVG + จัด repo

- **รายการงานสไตล์ใหม่** (`ab32c70`) — icon สถานะ + badge + ปุ่มขวา + สรุป "N รายการ · ค้าง M"
  เรียงงาน วันนี้ → เกินกำหนด → รอตรวจ → เสร็จสิ้น
  - สถานะ map จากข้อมูลจริง (ยังไม่มี "กำลังตรวจ"/draft): วันนี้=ไฮไลต์น้ำเงิน, เกินกำหนด=แดง,
    รอตรวจ=เทา, เสร็จสิ้น=เขียว (ปุ่ม "ดูผล")
- **แก้ icon หาย** (`f944bb9`) — เปลี่ยนไอคอนสถานะจาก icon-font เป็น **inline SVG** (`fill:currentColor`)
  แสดงผลชัวร์ทุกเครื่อง/ไม่พึ่งฟอนต์
- **จัด repo** (`f944bb9`, `38e9afb`, `18ce09f`) — เพิ่ม `.gitignore`, ถอด `export/`, ไฟล์ส่วนตัว
  (`.obsidian/`, scripts, `.docx`, `.csv`), และลบหน้ากำพร้า `admin.html` (ถูกแทนด้วย home.html)

## 11. ตรวจทั้งระบบ (flow / การคำนวณ / data layer) + แก้จุดที่พบ

ไล่ตรวจ 3 ด้านด้วย subagent — สรุป: ระบบหลักแข็งแรง (routing, param ระหว่างหน้า, สูตรคะแนน,
การตัด N/A, เกณฑ์ 90/75, field names สอดคล้องกันหมด) ไม่มีบั๊ก critical

**การตัดสินใจ:** ให้ KPI เป็น **ภาพรวมทั้งบริษัท** + แก้จุดเล็กน้อยทั้งหมด (`a9f4c1d`)

- **KPI ทั้งบริษัท (RLS)** — เดิม auditor เห็น audit ของตัวเองเท่านั้น (RLS `auditor_id=auth.uid()`)
  → เปิดสิทธิ์ "อ่าน" `audit_headers` / `audit_details` / `profiles` ให้ผู้ล็อกอินทุกคน
  (การ "เขียน" ยังจำกัดเจ้าของ/admin เหมือนเดิม) → Home/Dashboard/History เห็นทั้งบริษัท,
  ชื่อผู้ตรวจร่วมแสดงครบ
- **summary กรณี N/A ทั้งหมด** (max=0) → แสดง "ไม่มีข้อประเมิน" แทน "Need Improvement 0%"
- **History** ตัด audit ที่ยังไม่สมบูรณ์ (status=pending) ออก ให้สอดคล้อง dashboard
- **แถบสี ranking/trend** ตัดสินจากคะแนนจริง (`avgScoreRaw`) กันปัดเศษ .5 ข้ามแถบสี
- **guard กัน submit** เมื่อ session หลุด (กัน `auditor_id='unknown'` ที่ insert ไม่ผ่าน RLS)
- ลบ dead code `setEl('totalMaxScore')` + แก้ comment เมนูใน home

---

## Migration ที่ต้องรันบน Supabase (รันแล้ว)

| ไฟล์ | ทำอะไร |
|---|---|
| `patches.sql` ส่วน D | เปิดสิทธิ์อ่าน headers/details/profiles ให้ทุก auth (KPI ทั้งบริษัท) |

> ⚠️ ผลข้างเคียง: ผู้ใช้ภายในอ่านตาราง `profiles` ได้ทั้งหมด (รวม email/role) — ถ้าต้องซ่อน
> ให้เปลี่ยนเป็น view เปิดแค่ id+name ภายหลัง

## เวอร์ชันไฟล์ (ล่าสุด)

- `app.js` / `style.css` → `v=32`
- service worker cache → `v4.5`

## หมายเหตุ SQL (สำคัญ)

- DB ที่ใช้อยู่ → รันแค่ `patches.sql` (ส่วน C = audit log, D = RPC reset, D = KPI RLS)
- **ห้ามรัน `schema.sql` บน DB ที่ใช้อยู่** (จะ error `already exists`) — มีไว้สำหรับ DB ใหม่เท่านั้น

## ค้างไว้ทำต่อ

- ป้าย **N/A** ในหน้า summary → ✅ ทำแล้ว (กรณี N/A ทั้งหมด)
- (ถ้าต้องการ) ฟีเจอร์ "ตรวจต่อ" — เก็บร่างการตรวจที่ยังไม่ส่ง (ตอนนี้มีแค่ pending/completed)
- (ถ้าต้องการ) จำกัด `profiles` อ่านเฉพาะ id+name ผ่าน view เพื่อความเป็นส่วนตัว
- (ถ้าต้องการ) เก็บ log ละเอียดระดับ audit_details, หรือ export log เป็นไฟล์
