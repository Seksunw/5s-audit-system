# สรุปงาน — 5S Audit System

**วันที่:** 30 กรกฎาคม 2026
**Repo:** `Seksunw/5s-audit-system` (branch `main`) — GitHub บัญชีใหม่
**Live:** https://seksunw.github.io/5s-audit-system/

---

## หัวข้อหลัก: ย้าย Backend จาก Google Apps Script + Google Sheets → Supabase

เดิมระบบใช้ Google Apps Script เป็น API + Google Sheets เป็นฐานข้อมูล + imgBB เก็บรูป
ย้ายทั้งหมดมาที่ **Supabase** (PostgreSQL + Auth + Storage) และย้าย repo ไป GitHub บัญชีใหม่

---

## 1. ฐานข้อมูล (Supabase Postgres)

สร้าง schema จาก 9 sheets เดิม → 8 ตาราง Postgres (`supabase/schema.sql`)

- ตาราง: `profiles`, `plants`, `areas`, `criteria`, `audit_headers`, `audit_details`, `schedules`, `audit_logs`
- ตัด `Sessions` sheet ทิ้ง — Supabase Auth จัดการ session/JWT เอง
- ใช้ **ENUM** แทน string อิสระ (role, status, area_type, audit_status)
- **RLS (Row Level Security)** ทุกตาราง — แก้ปัญหา IDOR + role scoping ที่ระดับ DB
- **Trigger** คำนวณ Total_Score / Percent / Status อัตโนมัติจาก audit_details — แก้ปัญหา submit ไม่ atomic
- **Trigger** auto-create profile เมื่อมี auth user ใหม่

## 2. ข้อมูลตั้งต้น (`supabase/seed_master.sql`)

Generate จาก `Criteria_Master.csv` + ข้อมูลใน `Code.gs` เดิม
- plants 3, areas 36, criteria 132 (แปลง Area_Type → enum array อัตโนมัติ)

## 3. Storage + Admin (`supabase/storage_and_first_admin.sql`)

- สร้าง bucket `audit-photos` (public) + policy: ผู้ล็อกอินอัปโหลดได้ / อ่าน public
- ตั้ง Admin คนแรก + ตั้ง role ผู้ใช้

## 4. Frontend (`js/app.js`)

เขียน **adapter** แทน Google Apps Script โดย**ไม่ต้องรื้อหน้าเว็บ**
- แทน `API.get/post` (fetch GAS) ด้วย `supabase-js` + handler ครบทุก action
- adapter แปลง response (snake_case) → key เดิม (PascalCase) ที่หน้าเว็บใช้อยู่
- `Session` → ใช้ Supabase Auth (login / signOut / JWT)
- อัปโหลดรูป: imgBB → **Supabase Storage** (ใช้ชื่อฟังก์ชัน `uploadToImgBB` เดิมไว้ไม่ต้องแก้ submit)
- ลบ secret เก่าออกหมด (GAS URL, imgBB key) เหลือ Supabase URL + publishable key (ปลอดภัยกับ RLS)
- เพิ่ม `<script>` supabase-js CDN ทุกหน้า HTML, bump `app.js?v=10`, sw cache `v2.3`

## 5. ย้าย GitHub

- สร้าง repo ใหม่ `Seksunw/5s-audit-system` (public — จำเป็นสำหรับ GitHub Pages ฟรี)
- เปลี่ยน git remote + แก้ชื่อ user เก่า (`seksunw58-ai`) ในไฟล์ tooling
- push ขึ้น repo ใหม่ + เปิด GitHub Pages (branch `main`)

---

## ปัญหาเดิมที่แก้ได้จากการย้าย (จาก ARCHITECTURE_AUDIT 63/100)

| เดิม | แก้ด้วย |
|---|---|
| IDOR — แก้ audit คนอื่นได้ | RLS policy ระดับแถว |
| ไม่มี LockService → race condition | Postgres transaction |
| submit ไม่ atomic เหลือ header ค้าง | trigger คำนวณ + upsert |
| 401 มองไม่เห็น (HTTP 200 หมด) | HTTP status จริง + JWT |
| password ไม่มี salt / ส่งผ่าน URL | Supabase Auth (bcrypt + JWT) |
| imgBB key โผล่ในโค้ด | Supabase Storage |
| ต้อง deploy GAS เองทุกครั้ง | หายไป |

## ที่ยังค้าง (optional)

- ปุ่ม "เพิ่มผู้ใช้" ในแอปยังสร้าง auth user ไม่ได้ (ต้องทำ Edge Function) — ตอนนี้เพิ่ม user ผ่าน Supabase dashboard ก่อน แล้วตั้ง role ในแอป/SQL
- ย้ายข้อมูล audit เก่าจาก Google Sheet (ถ้าต้องการเก็บประวัติ)
- แยกสิทธิ์ Manager / Area Manager ให้ชัดเจนขึ้น
- ย้าย GitHub token จาก `.git/config` ไปเก็บใน Keychain

---

## ไฟล์สำคัญ

| ไฟล์ | หน้าที่ |
|---|---|
| `supabase/schema.sql` | ตาราง + RLS + trigger |
| `supabase/seed_master.sql` | ข้อมูล plants/areas/criteria |
| `supabase/storage_and_first_admin.sql` | bucket รูป + role ผู้ใช้ |
| `SUPABASE_MIGRATION_PLAN.md` | แผนย้าย + mapping ละเอียด |
| `js/app.js` | frontend + Supabase adapter |
