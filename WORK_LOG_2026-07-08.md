# สรุปงาน — 5S Audit System

**วันที่:** 8 กรกฎาคม 2026
**Repo:** `Seksunw/5s-audit-system` (branch `main`)

---

## 1. รวมโฟลเดอร์ให้เหลือชุดเดียว

มีโปรเจกต์ซ้ำ 2 โฟลเดอร์ (`5S apps` และ `5s-audit-system`) เป็นแอปเดียวกันคนละเวอร์ชัน

- โค้ดหลัก (`Code.gs`, `app.js`, `style.css`) เหมือนกัน — ต่างแค่ HTML/PWA shell
- เลือกใช้ **`5s-audit-system`** เป็นตัวจริง (มี git history + docs) แล้วเอาไฟล์แอปเวอร์ชันใหม่จาก `5S apps` มารวม + ไฟล์เสริมทั้งหมด
- สำรองไฟล์เดิมก่อนแก้ไว้ที่ `backup_5s-audit-system_...`

---

## 2. Audit สถาปัตยกรรมทั้งระบบ

อ่านทุกไฟล์ (`Code.gs` 1,211 บรรทัด, `app.js` 2,816 บรรทัด, HTML 11 หน้า, sw.js, manifest, docs) แล้วทำรายงานเต็ม 14 หัวข้อ → `ARCHITECTURE_AUDIT_2026.md`

**คะแนนรวม: 63/100** ปัญหาสำคัญที่เจอ:

| ระดับ | ปัญหา |
|---|---|
| 🔴 | ไม่จัดการ 401 ฝั่ง client → session หมดอายุแล้วค้าง (ต้นเหตุอาการ Unauthorized) |
| 🔴 | IDOR — ใครก็แก้/finalize audit ของคนอื่นได้ |
| 🔴 | ไม่มี LockService → race condition ตอนหลายคน submit |
| 🔴 | submit ไม่ atomic → เหลือ header ค้าง |
| 🔴 | credential โผล่ในโค้ด (admin quick-login + imgBB key) |
| 🟠 | โค้ด Drive เป็น dead code (จริงๆ ใช้ imgBB) |

---

## 3. แก้บั๊ก Critical (5 ข้อ)

**`app.js`**
- เพิ่ม `API._request()` จับ `code:401` → เคลียร์ session + เด้งกลับ login
- `submitAudit()` มี rollback (`deleteAudit`) ถ้าล้มกลางทาง
- cache key ผูกกับ token

**`Code.gs`**
- `submitAuditDetails` / `finalizeAudit` เช็ค `assertAuditOwner_` (แก้ IDOR)
- เพิ่ม `withLock_()` ครอบทุกการเขียน (กัน race condition)
- เพิ่ม endpoint `apiDeleteAudit`

**`index.html`** — ลบปุ่ม quick-login admin ออก (ไม่มี `admin1234` ในโค้ดแล้ว)

---

## 4. แก้บั๊กหน้า Schedule / เมนู Admin

พบว่า `Session.load()` คืนค่า boolean แต่โค้ด 3 จุด (`initHome`, `initPlant`, `initSchedule`) ใช้เหมือนเป็น object → `role`/`userId` เป็น `undefined` เสมอ ทำให้:

- เมนู "มอบหมาย" ไม่ขึ้นสำหรับ admin
- admin เปิดหน้า schedule ไม่ได้ (โดนเด้งกลับ)
- auditor ไม่เห็นการ์ด "งานที่ได้รับมอบหมาย"

**แก้:** อ่าน role/userId จาก `AppState.user` ทั้ง 3 จุด + ปรับให้ admin เห็นทั้งเมนู "มอบหมาย" และ "ผู้ใช้" พร้อมกัน (เดิมสลับกันในช่องเดียว)

---

## 5. เพิ่มฟีเจอร์ลบผู้ใช้

เดิมมีแค่ เพิ่ม/แก้ไข ไม่มีลบ

- **Backend:** เพิ่ม `apiDeleteUser` — ลบจริง (hard delete) หายจาก Google Sheet ด้วย, admin-only, ครอบ lock
- **Safety:** กันลบบัญชีตัวเอง + กันลบ Admin คนสุดท้ายที่ Active
- **Frontend:** ปุ่มแดง "ลบผู้ใช้นี้" ในกล่องแก้ไข + popup ยืนยัน

ตอนนี้ครบวงจร: เพิ่ม / แก้ไข / ลบ ทำได้ทั้งบนแอปและสะท้อนไป Google Sheet

---

## Commits วันนี้

| Commit | รายละเอียด |
|---|---|
| `66dde50` | fix: 401, IDOR, LockService, atomic rollback, ลบ quick-login, bump v6/v1.9 |
| `43d3859` | (rebase รวมกับ remote) |
| `7074a79` | fix: เมนู schedule ของ admin หาย + หน้า schedule เด้ง |
| `b324514` | feat: ลบผู้ใช้ (hard delete จากแอป + Google Sheet) |

เวอร์ชันล่าสุด: `app.js?v=9`, service worker `v2.2`

---

## ⚠️ ต้องทำเองที่ Apps Script

Backend เป็น Google Apps Script คนละที่กับ repo — ทุกครั้งที่แก้ `Code.gs` ต้อง:
**Deploy → Manage deployments → Edit → Version: New version → Deploy** (ใช้ URL เดิม)

*(ทำแล้วสำหรับรอบวันนี้)*

---

## ยังค้างไว้ (นอกขอบเขตที่ทำวันนี้)

- imgBB key ยังโผล่ในโค้ด (เลือกคง imgBB ไว้ก่อน)
- duplicate `toggleCategory()` ใน `app.js`
- icon PWA หาย (`icon-192.png`) → ติดตั้งแอปไม่สมบูรณ์
- role scoping — history/dashboard เห็นข้อมูลทุก plant ทุก role
- password ส่งผ่าน URL ตอน login + hash ไม่มี salt
